#999 — Cluster outage blocking k8s-runner deployment

Status: Open
Owner: @kushin77

Summary
-------
Automated validation found the kubeconfig context `production-context` pointing
to `https://192.168.168.42:6443`, which refused connections during checks on
March 6, 2026. This prevents in-cluster operations (SealedSecret apply, Helm
install, pod verification) and blocks the hands-off GitLab Runner deployment.

Evidence
--------
- `~/.kube/config` current-context: `production-context`
- cluster server: `https://192.168.168.42:6443`
- automated check result: connection refused (TCP RST) when contacting the API server

Blocking Impact
---------------
- Cannot apply SealedSecret or Secret to target cluster from this host
- Cannot run `./scripts/ci/hands_off_runner_deploy.sh` (requires reachable cluster)
- Cannot validate runner readiness with `scripts/ci/validate_runner_readiness.sh`

Required Actions
----------------
1. Restore API server at `192.168.168.42:6443` or provide an alternate kubeconfig/context with a reachable control plane.
2. Provide the short-lived registration token (`REG_TOKEN`) or apply a SealedSecret for `gitlab-runner-regtoken` in the `gitlab-runner` namespace.
3. (Optional) If cluster fix will take time, provide permission to provision a local KinD/k3d cluster on this host for smoke-testing. This requires Docker and install permissions.

Suggested Next Steps (once unblocked)
------------------------------------
- Apply the SealedSecret (preferred) or Secret (test-only)
- Run `./scripts/ci/hands_off_runner_deploy.sh` to perform Helm install
- Run `scripts/ci/validate_runner_readiness.sh` and `scripts/ci/trigger_yamltest_pipeline.sh`

Notes
-----
This issue was auto-generated by deployment automation. It should be closed once a reachable kubeconfig and token are provided and the hands-off deployment completes successfully.
