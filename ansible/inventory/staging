[runners]
# Production runner host (correct worker node)
# Previously pointed to localhost which caused deployments to target the self-hosted runner (192.168.168.31).
# Change to the intended worker node IP so Ansible targets the correct host during automated deploys.
192.168.168.42 ansible_user=cloud ansible_become=true
