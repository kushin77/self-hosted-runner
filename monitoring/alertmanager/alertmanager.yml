global:
  resolve_timeout: 5m

route:
  receiver: ops
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: ops
    email_configs:
      - to: support@elevatediq.ai
        send_resolved: true

templates: []
