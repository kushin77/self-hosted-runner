# Azure Scale Set module

This module provisions a basic Azure Virtual Machine Scale Set to be used
as a runner pool. It's intentionally minimal and meant as a scaffold to
be extended for image, networking, and autoscaling policies.

See `examples/` for usage.
