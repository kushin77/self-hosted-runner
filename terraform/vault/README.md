# Terraform module: vault (scaffold)

Scaffold terraform module to deploy Vault into Kubernetes. Follow-ups should add Helm release, auto-unseal (KMS), storage backend configuration, and example injector patterns.
