# Terraform module: observability (scaffold)

Scaffold terraform module to deploy observability components (Prometheus/Grafana/Loki) into Kubernetes. Follow-ups should add Helm releases, PVCs, RBAC, and alerting rules.
