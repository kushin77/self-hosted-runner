# Terraform module: minio (scaffold)

This is a scaffold terraform module to deploy MinIO to Kubernetes. It intentionally contains a minimal namespace resource as a starting point.

Follow-ups:
- Add StatefulSet or Helm release resource.
- Add PVC, TLS, and monitoring/exporter resources.
