Terraform plan for infra/secrets-orchestrator

/--- plan start ---/

Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
  + create

Terraform will perform the following actions:

  # google_iam_workload_identity_pool.secrets_pool will be created
  + resource "google_iam_workload_identity_pool" "secrets_pool" {
      + display_name              = "secrets-orchestrator-pool"
      + id                        = (known after apply)
      + name                      = (known after apply)
      + project                   = "nexusshield-prod"
      + state                     = (known after apply)
      + workload_identity_pool_id = "secrets-pool"
    }

  # google_iam_workload_identity_pool_provider.secrets_provider will be created
  + resource "google_iam_workload_identity_pool_provider" "secrets_provider" {
      + display_name                       = "secrets-orchestrator-provider"
      + id                                 = (known after apply)
      + name                               = (known after apply)
      + project                            = "nexusshield-prod"
      + state                              = (known after apply)
      + workload_identity_pool_id          = "secrets-pool"
      + workload_identity_pool_provider_id = "secrets-provider"

      + oidc {
          + issuer_uri = "https://token.actions.githubusercontent.com"
        }
    }

  # google_kms_crypto_key.mirror_key will be created
  + resource "google_kms_crypto_key" "mirror_key" {
      + crypto_key_backend         = (known after apply)
      + destroy_scheduled_duration = (known after apply)
      + effective_labels           = (known after apply)
      + id                         = (known after apply)
      + import_only                = (known after apply)
      + key_ring                   = (known after apply)
      + name                       = "mirror-key"
      + primary                    = (known after apply)
      + purpose                    = "ENCRYPT_DECRYPT"
      + terraform_labels           = (known after apply)

      + version_template (known after apply)
    }

  # google_kms_key_ring.mirror will be created
  + resource "google_kms_key_ring" "mirror" {
      + id       = (known after apply)
      + location = "global"
      + name     = "nexusshield"
      + project  = "nexusshield-prod"
    }

Plan: 4 to add, 0 to change, 0 to destroy.

/--- plan end ---/
