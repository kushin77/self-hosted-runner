Generated SSH keypair for GitHub mirror rotation

- Private key location (local, do NOT commit): /tmp/dr_rotate_20260306T183538Z
- Public key: /tmp/dr_rotate_20260306T183538Z.pub

Next steps to complete rotation (requires secure secrets):

1. Provide `GITHUB_TOKEN` (PAT with repo write access) and `GITLAB_API_TOKEN` (GitLab API token) via Google Secret Manager or Vault.
2. Run the rotation script on an ops host with network access:

   GITHUB_REPO=org/repo GITHUB_TOKEN=... GITLAB_API_URL=... GITLAB_API_TOKEN=... GROUP_ID=1 \
   VAR_KEY=GITHUB_MIRROR_SSH_KEY ./scripts/ci/rotate_github_deploy_key.sh

3. Verify the new deploy key is present in the GitHub repo and that the private key is stored as a protected CI variable.
4. Revoke the previous deploy key if not already removed by the script.

Security notes:
- Never commit private keys. Move `/tmp/dr_rotate_20260306T183538Z` to a secure vault or ephemeral CI variable via the rotation script.
- Audit and rotate again after successful validation.
