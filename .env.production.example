# NexusShield Portal - Production Environment Configuration
# ⚠️  WARNING: Replace all example values with real production credentials
# Store this file securely - it contains sensitive credentials

# ===== DATABASE CONFIG =====
DB_PASSWORD=your_secure_postgres_password_here
DATABASE_URL=postgresql://portal:${DB_PASSWORD}@postgres:5432/nexusshield

# ===== CACHE CONFIG =====
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379

# ===== GCP CONFIGURATION =====
GCP_PROJECT_ID=nexusshield-prod
GCP_KMS_KEY=projects/nexusshield-prod/locations/us-central1/keyRings/portal-kr/cryptoKeys/portal-key
# Use Workload Identity Federation instead of long-lived service account keys.
# Set these variables to enable WIF-based authentication in automation (preferred):
GCP_WORKLOAD_IDENTITY_PROVIDER=projects/PROJECT_NUMBER/locations/global/workloadIdentityPools/POOL/providers/PROVIDER
GCP_SERVICE_ACCOUNT_EMAIL=automation-sa@your-gcp-project.iam.gserviceaccount.com
# If an operator must provide a service account key temporarily, store it in GSM and
# fetch at runtime; do not set GOOGLE_APPLICATION_CREDENTIALS persistently.

# ===== API CONFIGURATION =====
NODE_ENV=production
PORT=3000
LOG_LEVEL=info
API_BASE_URL=http://backend:3000

# ===== FRONTEND CONFIG =====
REACT_APP_API_URL=http://localhost:3000
FRONTEND_PORT=3001

# ===== SECURITY =====
JWT_SECRET=your_jwt_secret_key_here_min_32_chars
CORS_ORIGIN=http://localhost:3001,https://yourdomain.com

# ===== FEATURE FLAGS =====
ENABLE_GSM_VAULT=true
ENABLE_KMS_ENCRYPTION=true
ENABLE_AUDIT_LOGGING=true
ENABLE_METRICS=true

# ===== OBSERVABILITY =====nPROMETHEUS_PORT=9090
METRICS_ENABLED=true
