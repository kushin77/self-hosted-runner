# Example environment variables for portal docker-compose
# DO NOT store real secrets here. Use GSM/Vault/KMS to fetch secrets at deploy time.

# Public API URL the frontend should call (use the worker's external IP/hostname)
VITE_API_URL=http://localhost:5000

# Logging level
VITE_LOG_LEVEL=info

# NODE env for API
NODE_ENV=production
