name: Force Terraform Plan

on:
  workflow_dispatch:
            const workflow_id = 'terraform-plan.yml';
            const ref = (context.payload && context.payload.inputs && context.payload.inputs.ref) || context.ref || 'main';
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id,
              ref
            });
        description: 'Branch or ref to run terraform-plan on'
        required: false
        default: 'main'

permissions:
            const runRef = (context.payload && context.payload.inputs && context.payload.inputs.ref) || context.ref || 'main';
            const body = `Triggered terraform-plan on ref: ${runRef}`;
  actions: write
  issues: write

jobs:
  dispatch-plan:
    name: Dispatch Terraform Plan
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch terraform-plan workflow on target ref
        uses: actions/github-script@v6
        with:
          script: |
<<<<<<< HEAD
            const repo = context.repo.owner + '/' + context.repo.repo;
            const workflow_id = 'terraform-plan.yml';
            const ref = core.getInput('ref') || github.context.payload.ref || 'main';
=======
            const workflow_id = 'terraform-plan.yml';
            const ref = (context.payload && context.payload.inputs && context.payload.inputs.ref) || context.ref || 'main';
>>>>>>> 5bf5925a1 (ci: fix force-terraform-plan github-script usage)
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id,
              ref
            });
      - name: Post note to ops issue
        uses: actions/github-script@v6
        with:
          script: |
<<<<<<< HEAD
            const runRef = core.getInput('ref') || github.context.payload.ref || 'main';
=======
            const runRef = (context.payload && context.payload.inputs && context.payload.inputs.ref) || context.ref || 'main';
>>>>>>> 5bf5925a1 (ci: fix force-terraform-plan github-script usage)
            const body = `Triggered terraform-plan on ref: ${runRef}`;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: 1384,
              body
            });
