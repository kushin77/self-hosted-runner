package runner.policy

# deny stub for policy enforcement and signing requirement
deny[msg] {
  msg := "Policy enforcement placeholder: allow by default"
}

# signing requirement note
# signing_required enforced elsewhere (placeholder)
