# Control Plane Reference Server (PoC)

This is a minimal PoC server implementing the registration endpoint defined in `openapi.yaml`.

Note: For production, terminate TLS with a proper mTLS-enabled proxy (NGINX/Envoy) and run the app behind it.
