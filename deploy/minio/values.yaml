replicaCount: 3

image:
  repository: minio/minio
  tag: RELEASE.2026-01-01
  pullPolicy: IfNotPresent

persistence:
  enabled: true
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 100Gi

service:
  type: ClusterIP
  port: 9000

resources: {}

securityContext: {}

# TLS and credentials should be supplied via external secret management in production
