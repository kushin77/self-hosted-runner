# Recommended GitHub Issues for Canonical Secrets Enhancement

This file lists suggested issues to create/track in your GitHub repository. I created this locally; I can open real GitHub issues if you provide the `owner/repo` and an access token.

## Suggested issues

1. chore: Review and approve Vault-primary canonical secrets implementation
   - Description: Review `scripts/secrets/*`, `scripts/cloudrun/canonical_secrets_provider.py`, `backend/src/api/canonical_secrets_api.py`, `BASE64_BLOB_REDACTED.tsx`, and `scripts/nexusshield-secrets-cli.sh` for security and operational readiness.
   - Labels: enhancement, security-review

2. test: Add integration smoke tests for secrets provider resolution
   - Description: Add CI-free integration tests that run locally (no GitHub Actions) to validate provider resolution logic and canonical sync.
   - Labels: test, integration

3. ops: Deploy FastAPI canonical_secrets_api to staging
   - Description: Prepare staging environment, configure env vars, run `uvicorn canonical_secrets_api:app` or containerize and deploy.
   - Labels: ops, staging

4. ops: Deploy Portal (React) with SecretsManagementDashboard
   - Description: Build and deploy frontend; ensure `API_ENDPOINT` and auth are configured.
   - Labels: ops, ui

5. security: Run pen-test and audit of audit-trail immutability
   - Description: Validate hash-chain log storage and export; ensure append-only enforcement.
   - Labels: security, compliance

6. docs: Add runbook for migration orchestrator and rollback
   - Description: Document step-by-step recovery, backup, and rollback procedures for `canonical-migration-orchestrator.sh`.
   - Labels: documentation, runbook

7. infra: Add health-monitoring alerts for Vault/GSM/AWS/Azure
   - Description: Configure monitoring and alert rules for provider health and migration failures.
   - Labels: infra, monitoring

8. chore: Remove GitHub Actions & PR release steps from repo
   - Description: Ensure repository has no GitHub Actions workflows; add policy docs to block their use.
   - Labels: maintenance, policy

## How I can help next
- Create GitHub issues automatically if you provide `owner/repo` and a token (or grant `gh` CLI access here).
- Push the local branch to your remote if you provide remote credentials or allow me to use an existing remote.
- Wire deployment (systemd unit, container, or direct `uvicorn`) and run smoke tests.


---
Generated by NexusShield automation on 2026-03-11.